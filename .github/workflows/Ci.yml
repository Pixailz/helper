name: CI

run-name: Full helper CI/CD

on:
  push:
    branches: [main]
    # paths:
    #   - 'helper'
    #   - 'modules/**/**.py'
  pull_request:
    types: [opened]

jobs:
  # info:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: "github"
  #       run: |+
  #         echo "Who launched: ${{github.actor}}"
  #         echo "On branch:    ${{github.ref_name}}"
  #         echo "Commit sha:   ${{github.sha}}"

  #     - name: "Bin utils"
  #       run: |+
  #         make --version && echo
  #         ar --version && echo
  #         gcc --verbose && echo
  #         python3 --version

  test:
    runs-on: ubuntu-latest
    steps:
      - name: Retriev ft_libft repo
        run: |+
          mkdir test && cd test
          git clone https://github.com/Pixailz/ft_libft && cd ft_libft/rsc
          rm -rf helper && git clone https://github.com/Pixailz/helper && cd ..
          make
